spring.application.name=birthday-backend

# ✅ Use dynamic port on Render, fallback to 8080 locally
server.port=${PORT:8080}

# ❌ Remove server.address (Render will auto-bind correctly)

# 🎂 Your custom configs
birthday.date=2025-11-16T00:00:00
birthday.password=Pageliloveyou
